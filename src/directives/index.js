const {
  authenticationDirectiveTransformer,
  authorizationDirectiveTransformer,
} = require('./auth')

module.exports = {
  authenticationDirectiveTransformer,
  authorizationDirectiveTransformer,
}
